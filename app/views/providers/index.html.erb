<p id="notice"><%= notice %></p>

<h1>Fornecedores</h1>

<table>
  <thead>
    <tr>
      <th>Nome</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @providers.each do |provider| %>
      <tr>
        <td><%= provider.name %></td>
        <td><%= link_to t('views.form.actions.show'), provider %></td>
        <td><%= link_to t('views.form.actions.edit'), edit_provider_path(provider) %></td>
        <td><%= link_to t('views.form.actions.destroy'), provider, method: :delete, data: { confirm: 'Têm certeza?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to t('views.form.actions.new'), new_provider_path %>
